[project]
name = "dataloom"
version = "0.1.0"
description = "Safe-by-default research automation pipeline skeleton"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = []

[project.scripts]
dataloom-demo = "run_demo:main"

[tool.ruff]
line-length = 100
target-version = "py310"
